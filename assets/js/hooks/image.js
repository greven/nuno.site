export const Image = {
  mounted() {
    // const image = this.el;
    // const imageId = this.el.getAttribute('id');
    // if (imageHash) {
    // image.onload = () => {
    // imageHash.classList.add('hidden');
    // image.classList.remove('hidden');
    // };
    // }
  },
};
