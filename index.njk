---
layout: layouts/home.njk
eleventyNavigation:
  key: Home
  order: 1
---

{% block content %}
  <div class="text-cool-gray-50">
    <div class="fixed inset-0 flex flex-col justify-center items-center bg-dark-900">
      <div>
        <div x-data="{message:'🤖 Hello World 🤓'}" x-text="message"></div>
        <div>Might actually build this, this time around...</div>
      </div>

      <div class="mt-8">
        <h1 class="text-4xl font-bold">Latest 3 Posts</h1>

        {% set postslist = collections.posts | head(-3) %}
        {% set postslistCounter = collections.posts | length %}
        {% include "postslist.njk" %}

        <p>More posts can be found in
          <a href="{{ '/posts/' | url }}">the archive</a>.</p>
      </div>
    </div>
  </div>
{% endblock %}
